# freshshell.com
fresh freshshell/fresh bin/fresh --bin

# remote
fresh git/git contrib/completion/git-completion.bash

# config
fresh config/\* --file

# shell
fresh shell/\*

# helpers
fresh helpers/\*

# local (non-public)
fresh local/\*
